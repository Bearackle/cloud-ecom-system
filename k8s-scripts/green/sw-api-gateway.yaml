apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: ecom
  labels:
    app: api-gateway
spec:
  selector:
    app: api-gateway
    version: green
  ports:
    - port: 8303
      targetPort: 8303
      protocol: TCP
      name: http
  type: ClusterIP
